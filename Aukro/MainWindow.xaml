<Window x:Class="Aukro.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Aukro"
        xmlns:vm="clr-namespace:Aukro.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <StackPanel Margin="0,0,0,368" Orientation="Horizontal">
            <Button Click="Login_Click" Content="Přihlášní" Width="116" Margin="15,0,15,0" Height="41"/>
            <Button Click="Register_Click" Content="Registrace" Width="116" Margin="15,0,15,0" Height="41"/>
            <Button Click="Logout_Click"  IsEnabled="{Binding IsLoggedIn, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Content="Odhlásit se" Width="116" Margin="15,0,15,0" Height="41" Foreground="Black"/>
            <TextBlock TextWrapping="Wrap" Text="{Binding CurrentUser, Mode=OneWay, UpdateSourceTrigger=PropertyChanged }" VerticalAlignment="Center" Foreground="Black"/>
        </StackPanel>


    </Grid>
</Window>
